# GeneInsight Lite

A lightweight bioinformatics web application that generates a quick gene report using public data sources.

## Run locally

```bash
pip install -r requirements.txt
uvicorn app:app --reload
